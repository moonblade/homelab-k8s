apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kestra-external
  labels:
    app.kubernetes.io/instance: kestra
    app.kubernetes.io/name: kestra
spec:
  parentRefs:
    - name: main-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - kestra.moonblade.work
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ak-outpost-kestra
          namespace: authentik
          port: 9000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kestra-internal
  labels:
    app.kubernetes.io/instance: kestra
    app.kubernetes.io/name: kestra
spec:
  parentRefs:
    - name: main-gateway
      namespace: nginx-gateway
      sectionName: https
  hostnames:
    - kestra.sirius.moonblade.work
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: kestra
          port: 8080
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kestra-internal-redirect
  labels:
    app.kubernetes.io/instance: kestra
    app.kubernetes.io/name: kestra
spec:
  parentRefs:
    - name: main-gateway
      namespace: nginx-gateway
      sectionName: http
  hostnames:
    - kestra.sirius.moonblade.work
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: kestra-internal-dns
  labels:
    app.kubernetes.io/instance: kestra
    app.kubernetes.io/name: kestra
spec:
  endpoints:
    - dnsName: kestra.sirius.moonblade.work
      recordTTL: 60
      recordType: A
      targets:
        - 192.168.29.200
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: kestra-outer
  labels:
    app.kubernetes.io/instance: kestra
    app.kubernetes.io/name: kestra
spec:
  endpoints:
    - dnsName: kestra.moonblade.work
      recordTTL: 60
      recordType: CNAME
      targets:
        - 46b0a967-3b0f-493a-88e7-6f1cc9f8852b.cfargotunnel.com
      providerSpecific:
        - name: external-dns.alpha.kubernetes.io/cloudflare-proxied
          value: 'true'
