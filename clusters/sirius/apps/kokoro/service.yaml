apiVersion: v1
kind: Service
metadata:
  name: kokoro
  labels:
    app: kokoro
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
  selector:
    app: kokoro
