# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: app-jackett
  labels:
    blueprints.goauthentik.io/description: "Jackett proxy application"
entries:
  - model: authentik_providers_proxy.proxyprovider
    id: jackett-provider
    state: present
    identifiers:
      name: Provider for Jackett
    attrs:
      name: Provider for Jackett
      mode: proxy
      external_host: https://jackett.moonblade.work
      internal_host: http://jackett.jackett.svc.cluster.local:9117
      intercept_header_auth: true
      access_token_validity: hours=24
      refresh_token_validity: days=30
      authentication_flow: !Find [authentik_flows.flow, [slug, google-login]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]

  - model: authentik_core.application
    id: jackett-app
    state: present
    identifiers:
      slug: jackett
    attrs:
      name: Jackett
      slug: jackett
      provider: !KeyOf jackett-provider
      policy_engine_mode: any
      meta_launch_url: https://jackett.moonblade.work

  # Policy binding to allow users with admin role
  - model: authentik_policies.policybinding
    id: jackett-policy-binding
    state: present
    identifiers:
      order: 0
      target: !KeyOf jackett-app
    attrs:
      policy: !Find [authentik_policies_expression.expressionpolicy, [name, "Has role \"admin\""]]
      target: !KeyOf jackett-app
      order: 0
      enabled: true
      timeout: 30

  - model: authentik_outposts.outpost
    id: jackett-outpost
    state: present
    identifiers:
      name: Jackett
    attrs:
      name: Jackett
      type: proxy
      service_connection: !Find [authentik_outposts.kubernetesserviceconnection, [name, Local Kubernetes Cluster]]
      providers:
        - !KeyOf jackett-provider
      config:
        authentik_host: https://login.moonblade.work/
        kubernetes_namespace: authentik
        kubernetes_replicas: 1
        kubernetes_service_type: ClusterIP
        kubernetes_disabled_components:
          - Ingress
        object_naming_template: ak-outpost-%(name)s
        log_level: info
